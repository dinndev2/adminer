<nav class="rounded-lg mt-5">
  <ul class="flex flex-col gap-3 w-28">
    <li>
      <%= link_to user_settings_path, data: {turbo_action: "advance", turbo_frame: "settings-content"},class: "text-xs text-gray-900 rounded-lg py-2 px-4 hover:bg-white transition-all ease-in flex gap-4 items-center" do %>
        <%= inline_svg_tag "users.svg", class: "w-5 h-5" %>
        Users
      <% end %>
    </li>
  </ul>
</nav>